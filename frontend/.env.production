# Продакшен (npm run build) — поменяйте IP/домен на адрес вашего сервера
# Пример: http://192.168.1.50:8080  или  https://kiosk.example.com
VITE_API_ORIGIN=http://YOUR_SERVER_IP:8080

# Если nginx проксирует /api на тот же домен, что и фронт — оставьте пустым:
# VITE_API_ORIGIN=
