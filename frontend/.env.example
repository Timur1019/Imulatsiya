# Скопируйте нужный файл:
#   .env.development  — для npm run dev
#   .env.production   — для npm run build на сервере
#
# Одна переменная — адрес backend без /api в конце:
VITE_API_ORIGIN=http://YOUR_SERVER_IP:8080
